package harmony.core.impl.fact;

import harmony.core.api.property.Property;
import harmony.core.api.thing.Thing;

public class BasicFact extends AbstractFact {

	public BasicFact(Property property, Thing... things) {
		super(property, things);
	}

}
